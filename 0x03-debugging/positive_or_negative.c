#include "main.h"
/**
 * main - Determine if a umber is positive, nnegative or zero.
 * 0 : is the number to be checked
 * Return: 0 on success
 */
void positive or negative(int i)
{
	(if i > 0)
		printf("%d is positive\n", i);
	else if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is zero\n", i);
}	
